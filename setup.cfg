[tool:pytest]
addopts=
    --cov=prajna
    --cov-report=term-missing
    --quiet

[coverage:report]
exclude_lines =
    if __name__ == .__main__.:
show_missing=True

[flake8]
max-line-length=120
exclude=.venv
per-file-ignores =
    **/*.py: F405
    tests/*.py: D101,D102
